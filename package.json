{
  "name": "@example/minimal",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --root demo dev",
    "start": "astro dev --root demo dev",
    "build": "astro build --root demo dev",
    "preview": "astro preview --root demo dev",
    "astro": "astro"
  },
  "devDependencies": {
    "@github/clipboard-copy-element": "^1.1.2",
    "@github/relative-time-element": "^4.2.0"
  },
  "dependencies": {
    "astro": "^2.0.5"
  },
  "workspaces": [
    "./packages/*",
    "./demo"
  ]
}
